Проект: C:\projects\pricepulse
Задача: добавить извлечение цены "со скидкой/с картой" (price_card) и зачёркнутой старой цены (old_price) во все парсеры.

=== OZON ===

В parsers/ozon.py в методе parse() после получения soup добавить:

1. Поиск цены с Ozon-картой (зелёный блок):
   - Селекторы: '[data-widget="webPrice"] span:first-child', 'div[data-testid="price-with-card"]', текст рядом с "С банками" или "Ozon"
   - Или regex: r'(\d[\d\s]+)\s*₽\s*С\s+банками'
   - Или ищи в HTML: "priceWithCard" или "cardPrice"

2. Поиск цены без карты (текущая):
   - Уже есть, оставить как price

3. Поиск старой цены (зачёркнутая):
   - Селектор: 'span[data-testid="old-price"]', 'span[style*="line-through"]', 'span[class*="old"]'
   - Или regex: r'(\d[\d\s]+)\s*₽' в блоках с line-through или классом old
   - JSON-LD: offers.get("highPrice")

Возвращать:
{
    "name": ...,
    "price": цена_без_карты (или минимальная актуальная),
    "price_card": цена_с_картой (если есть, иначе None),
    "old_price": зачёркнутая_цена (если есть, иначе None),
    ...
}

=== WILDBERRIES ===

- salePriceU / 100 → price (текущая)
- priceU / 100 → old_price (если > price, иначе None)
- Если есть "с WB Кошельком" или "с подпиской" — искать отдельно как price_card

=== YANDEX_MARKET ===

- Основная цена → price
- Зачёркнутая цена (span с line-through, класс old-price) → old_price
- Цена с подпиской/картой (если есть блок "с Яндекс Плюс") → price_card

=== LAMODA / SBERMEGAMARKET / ALIEXPRESS ===

- Текущая цена → price
- Зачёркнутая/старая цена → old_price
- Цена с картой/подпиской → price_card (если видно на странице)

=== UNIVERSAL PARSER ===

- Искать все числа с ₽ на странице через regex
- Если найдено 3 цены: min → price_card, средняя → price, max → old_price
- Если найдено 2 цены: min → price, max → old_price
- Если 1 цена: price = эта цена, остальные None

=== TEST OUTPUT ===

Обновить вывод в test_anticaptcha.py:

✅ Название | Текущая: X₽ | С картой: Y₽ | Старая: Z₽ | Скидка: N%

Где:
- Если price_card есть: Скидка считается от old_price к price_card
- Если price_card нет: Скидка считается от old_price к price

=== ТРЕБОВАНИЯ ===
1. Все парсеры возвращают price, price_card, old_price
2. price_card = None если цены с картой нет
3. old_price = None если зачёркнутой цены нет
4. После обновления запустить test_anticaptcha.py и показать вывод с реальными товарами