Проект: C:\projects\pricepulse
Задача: добавить универсальный fallback в конец каждого парсера, чтобы находить название и цену через meta og:title и regex, если основные селекторы не сработали.

=== ФАЙЛ 1: parsers/ozon.py (в методе parse, перед return) ===

Найти метод parse. Перед самым return добавить:

        # Fallback
        text = soup.get_text(separator=" ", strip=True)
        if result.get("name") == "Неизвестно":
            meta = soup.find("meta", property="og:title")
            if meta:
                result["name"] = meta.get("content", "Неизвестно").split('|')[0].strip()
        if result.get("price") is None:
            price_match = re.search(r'(\d[\d\s]+)\s*₽', text)
            if price_match:
                result["price"] = self._clean_price(price_match.group(1))
        if result.get("image") == "":
            meta_img = soup.find("meta", property="og:image")
            if meta_img:
                result["image"] = meta_img.get("content", "")

=== ФАЙЛ 2: parsers/wildberries.py (в методе parse, перед return) ===

Перед return добавить:

        # Fallback
        text = soup.get_text(separator=" ", strip=True)
        if result.get("name") == "Неизвестно":
            meta = soup.find("meta", property="og:title")
            if meta:
                result["name"] = meta.get("content", "Неизвестно").split('|')[0].strip()
        if result.get("price") is None:
            price_match = re.search(r'(\d[\d\s]+)\s*₽', text)
            if price_match:
                result["price"] = self._clean_price(price_match.group(1))
        if result.get("image") == "":
            meta_img = soup.find("meta", property="og:image")
            if meta_img:
                result["image"] = meta_img.get("content", "")

=== ФАЙЛ 3: parsers/yandex_market.py (в методе parse, перед return) ===

Перед return добавить:

        # Fallback
        text = soup.get_text(separator=" ", strip=True)
        if result.get("name") == "Неизвестно":
            meta = soup.find("meta", property="og:title")
            if meta:
                result["name"] = meta.get("content", "Неизвестно").split('|')[0].strip()
        if result.get("price") is None:
            price_match = re.search(r'(\d[\d\s]+)\s*₽', text)
            if price_match:
                result["price"] = self._clean_price(price_match.group(1))
        if result.get("image") == "":
            meta_img = soup.find("meta", property="og:image")
            if meta_img:
                result["image"] = meta_img.get("content", "")

=== ФАЙЛ 4: parsers/lamoda.py (в методе parse, перед return) ===

Перед return добавить:

        # Fallback
        text = soup.get_text(separator=" ", strip=True)
        if result.get("name") == "Неизвестно":
            meta = soup.find("meta", property="og:title")
            if meta:
                result["name"] = meta.get("content", "Неизвестно").split('|')[0].strip()
        if result.get("price") is None:
            price_match = re.search(r'(\d[\d\s]+)\s*₽', text)
            if price_match:
                result["price"] = self._clean_price(price_match.group(1))
        if result.get("image") == "":
            meta_img = soup.find("meta", property="og:image")
            if meta_img:
                result["image"] = meta_img.get("content", "")

=== ФАЙЛ 5: parsers/sbermegamarket.py (в методе parse, перед return) ===

Перед return добавить:

        # Fallback
        text = soup.get_text(separator=" ", strip=True)
        if result.get("name") == "Неизвестно":
            meta = soup.find("meta", property="og:title")
            if meta:
                result["name"] = meta.get("content", "Неизвестно").split('|')[0].strip()
        if result.get("price") is None:
            price_match = re.search(r'(\d[\d\s]+)\s*₽', text)
            if price_match:
                result["price"] = self._clean_price(price_match.group(1))
        if result.get("image") == "":
            meta_img = soup.find("meta", property="og:image")
            if meta_img:
                result["image"] = meta_img.get("content", "")

=== ФАЙЛ 6: parsers/aliexpress.py (в методе parse, перед return) ===

Перед return добавить:

        # Fallback
        text = soup.get_text(separator=" ", strip=True)
        if result.get("name") == "Неизвестно":
            meta = soup.find("meta", property="og:title")
            if meta:
                result["name"] = meta.get("content", "Неизвестно").split('|')[0].strip()
        if result.get("price") is None:
            price_match = re.search(r'(\d[\d\s]+)\s*₽', text)
            if price_match:
                result["price"] = self._clean_price(price_match.group(1))
        if result.get("image") == "":
            meta_img = soup.find("meta", property="og:image")
            if meta_img:
                result["image"] = meta_img.get("content", "")

=== ФАЙЛ 7: test_anticaptcha.py (обновить TEST_URLS) ===

Заменить TEST_URLS на:

TEST_URLS = [
    "https://www.ozon.ru/product/smartfon-apple-iphone-14-128gb-midnight-1040534597/",
    "https://www.wildberries.ru/catalog/15622747/detail.aspx",
    "https://market.yandex.ru/product--smartfon-apple-iphone-15-128gb-chernyi/1762667413",
    "https://www.lamoda.ru/p/mp002xw04f3j/clothes-nike-tolstovka/",
    "https://megamarket.ru/catalog/details/smartfon-xiaomi-redmi-note-12-128gb-onyx-gray-100030743080/",
    "https://aliexpress.ru/item/1005004953214053.html",
    "https://www.ozon.ru/product/naushniki-apple-airpods-pro-2-1040534600/",
    "https://www.wildberries.ru/catalog/150033123/detail.aspx",
    "https://market.yandex.ru/product--naushniki-apple-airpods-pro-2/1760234567",
    "https://www.lamoda.ru/p/mp002xw08qlo/clothes-acoola-tolstovka/",
]

=== ТРЕБОВАНИЯ ===
1. В каждом парсере (ozon, wildberries, yandex_market, lamoda, sbermegamarket, aliexpress) добавить fallback БЕЗ удаления существующего кода.
2. Fallback должен быть ПОСЛЕ основного парсинга и ПЕРЕД return.
3. Fallback ищет: og:title (название), regex (\d[\d\s]+)₽ (цена), og:image (картинка).
4. Обновить TEST_URLS на реальные товары.
5. После обновления запустить test_anticaptcha.py и показать полный вывод.